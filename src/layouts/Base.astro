---
import '../styles/global.css';
import { categories } from '../lib/content';
interface Props { title?: string; description?: string; current?: string; noindex?: boolean; }
const { title, description = 'イヤホン、充電器、デスク周り。毎日使うガジェットを、必要な機能から選ぶためのガイド。', current, noindex = false } = Astro.props;
const pageTitle = title ? `${title} | GADGET NOTE` : 'GADGET NOTE — 毎日使う道具を、よく選ぶ。';
const canonical = Astro.site ? new URL(Astro.url.pathname, Astro.site) : undefined;
const isPreview = import.meta.env.CF_PAGES_BRANCH && import.meta.env.CF_PAGES_BRANCH !== 'main';
---
<!doctype html>
<html lang="ja">
<head>
<meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1" />
<title>{pageTitle}</title><meta name="description" content={description} />
{canonical && <link rel="canonical" href={canonical} />}
{(noindex || isPreview) && <meta name="robots" content="noindex,nofollow" />}
<meta property="og:title" content={pageTitle} /><meta property="og:description" content={description} /><meta property="og:type" content="website" /><meta property="og:locale" content="ja_JP" />
{canonical && <meta property="og:url" content={canonical} />}
<link rel="icon" href="/favicon.svg" type="image/svg+xml" /><meta name="theme-color" content="#175bd7" />
</head>
<body>
<a class="skip" href="#main">本文へ移動</a>
<div class="topline"><div class="container">暮らしに合うガジェットの選び方</div></div>
<header class="header container">
<a class="brand" href="/" aria-label="GADGET NOTE ホーム"><span class="brand-mark">G<span>.</span></span><span>GADGET<br />NOTE<span class="brand-dot">.</span></span></a>
<p class="header-caption">毎日使う道具を、<br />よく選ぶ。</p>
</header>
<div class="nav-wrap"><nav class="container nav" aria-label="メインメニュー">
<a href="/" aria-current={current === 'home' ? 'page' : undefined}>すべての記事</a>
{categories.map(c => <a href={`/category/${c.id}/`} aria-current={current === c.id ? 'page' : undefined}>{c.name}</a>)}
</nav></div>
<main id="main"><slot /></main>
<footer class="footer"><div class="container footer-inner"><div><a class="footer-brand" href="/">GADGET NOTE<span>.</span></a><p>必要な機能を知って、納得して選ぶ。</p></div><a href="/about/">このサイトについて・編集方針</a><small>© {new Date().getFullYear()} GADGET NOTE</small></div></footer>
</body></html>
